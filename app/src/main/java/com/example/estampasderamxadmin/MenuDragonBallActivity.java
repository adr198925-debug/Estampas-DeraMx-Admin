package com.example.estampasderamxadmin;

import android.os.Bundle;
import androidx.appcompat.app.AppCompatActivity;

public class MenuDragonBallActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.menu_dragonball);

        // NO pusimos ningún Intent aquí porque borraste lo que seguía.
        // Así la app no buscará archivos que ya no existen.
    }
}